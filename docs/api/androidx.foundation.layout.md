<!-- GENERATED by dogwood-codegen from the component surface. Do not edit. -->
<!-- Regenerate with the segment's own generate task; see the module's build file. -->

# `androidx.foundation.layout` — component reference

Generated from the component surface, so it cannot disagree with what the generator
actually emitted. Version **1100300**, segment **253**.

A guest calls these as ordinary Kotlin Compose functions. Everything below describes
how each one crosses the boundary.

## Reading this

| Column | What it means |
| --- | --- |
| **Tag** | the number on the wire. Widget tags are `(segment shl 24) or local`, so this segment's are `253 shl 24` plus the local tag — which is how a number in a skew report becomes a name |
| **Kind** | how the parameter crosses: a property, a content slot, an event, or a live-state holder the host owns |
| **Default** | `host` means the guest sends nothing and the client picks. Absence is the sentinel, everywhere |

Two rules govern every number here, and both are enforced by the lock beside the surface:

- **Tags are permanent and append-only.** A renumbered tag does not fail to render; it
  renders the *wrong widget* on a client one version behind.
- **A parameter this client cannot read is not always harmless.** One marked
  **affordance** governs what the user is allowed to do — `enabled`, `checked`,
  `readOnly` — so a client meeting an unreadable one **withholds the whole widget**
  rather than drawing a control that lies about what it will do. Every other kind of
  skew degrades cosmetically. The components that own one are marked below.

## Components

| Component | Local tag | Widget tag | Owns an affordance |
| --- | ---: | ---: | :---: |
| [`Box`](#box) | 1 | -50331647 |  |
| [`Box~890fe8b6`](#box~890fe8b6) | 2 | -50331646 |  |
| [`BoxWithConstraints`](#boxwithconstraints) | 3 | -50331645 |  |
| [`Column`](#column) | 4 | -50331644 |  |
| [`FlowRow~cd63def4`](#flowrow~cd63def4) | 5 | -50331643 |  |
| [`FlowColumn~4f8a3c9f`](#flowcolumn~4f8a3c9f) | 6 | -50331642 |  |
| [`Row`](#row) | 7 | -50331641 |  |
| [`Spacer`](#spacer) | 8 | -50331640 |  |

---

### Box

Local tag 1, widget tag `-50331647`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `contentAlignment` | `Alignment` | 1 | `Alignment.TopStart` |  |
| `propagateMinConstraints` | `Boolean` | 2 | `false` |  |

**Content slots**

| Name | Tag |
| --- | ---: |
| `content` | 1 |

Takes a modifier chain.

---

### Box~890fe8b6

Local tag 2, widget tag `-50331646`.

Takes a modifier chain.

---

### BoxWithConstraints

Local tag 3, widget tag `-50331645`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `contentAlignment` | `Alignment` | 1 | `Alignment.TopStart` |  |
| `propagateMinConstraints` | `Boolean` | 2 | `false` |  |

**Content slots**

| Name | Tag |
| --- | ---: |
| `content` | 1 |

Takes a modifier chain.

---

### Column

Local tag 4, widget tag `-50331644`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `verticalArrangement` | `Arrangement.Vertical` | 1 | `Arrangement.Top` |  |
| `horizontalAlignment` | `Alignment.Horizontal` | 2 | `Alignment.Start` |  |

**Content slots**

| Name | Tag |
| --- | ---: |
| `content` | 1 |

Takes a modifier chain.

---

### FlowRow~cd63def4

Local tag 5, widget tag `-50331643`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `horizontalArrangement` | `Arrangement.Horizontal` | 1 | `Arrangement.Start` |  |
| `verticalArrangement` | `Arrangement.Vertical` | 2 | `Arrangement.Top` |  |
| `itemVerticalAlignment` | `Alignment.Vertical` | 3 | `Alignment.Top` |  |
| `maxItemsInEachRow` | `Int` | 4 | `Int.MAX_VALUE` |  |
| `maxLines` | `Int` | 5 | `Int.MAX_VALUE` |  |

**Content slots**

| Name | Tag |
| --- | ---: |
| `content` | 1 |

Takes a modifier chain.

---

### FlowColumn~4f8a3c9f

Local tag 6, widget tag `-50331642`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `verticalArrangement` | `Arrangement.Vertical` | 1 | `Arrangement.Top` |  |
| `horizontalArrangement` | `Arrangement.Horizontal` | 2 | `Arrangement.Start` |  |
| `itemHorizontalAlignment` | `Alignment.Horizontal` | 3 | `Alignment.Start` |  |
| `maxItemsInEachColumn` | `Int` | 4 | `Int.MAX_VALUE` |  |
| `maxLines` | `Int` | 5 | `Int.MAX_VALUE` |  |

**Content slots**

| Name | Tag |
| --- | ---: |
| `content` | 1 |

Takes a modifier chain.

---

### Row

Local tag 7, widget tag `-50331641`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `horizontalArrangement` | `Arrangement.Horizontal` | 1 | `Arrangement.Start` |  |
| `verticalAlignment` | `Alignment.Vertical` | 2 | `Alignment.Top` |  |

**Content slots**

| Name | Tag |
| --- | ---: |
| `content` | 1 |

Takes a modifier chain.

---

### Spacer

Local tag 8, widget tag `-50331640`.

Takes a modifier chain.


## Parameters a payload cannot set

Present in the library, absent from the stub: their type does not cross the boundary, so the
host passes the library's own default every time. Listed so nobody looks for a missing argument.

| Component | Parameter | Library type | Host passes |
| --- | --- | --- | --- |
