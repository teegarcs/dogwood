<!-- GENERATED by dogwood-codegen from the component surface. Do not edit. -->
<!-- Regenerate with the segment's own generate task; see the module's build file. -->

# `dogwood.designsystem` — component reference

Generated from the component surface, so it cannot disagree with what the generator
actually emitted. Version **12**, segment **1**.

A guest calls these as ordinary Kotlin Compose functions. Everything below describes
how each one crosses the boundary.

## Reading this

| Column | What it means |
| --- | --- |
| **Tag** | the number on the wire. Widget tags are `(segment shl 24) or local`, so this segment's are `1 shl 24` plus the local tag — which is how a number in a skew report becomes a name |
| **Kind** | how the parameter crosses: a property, a content slot, an event, or a live-state holder the host owns |
| **Default** | `host` means the guest sends nothing and the client picks. Absence is the sentinel, everywhere |

Two rules govern every number here, and both are enforced by the lock beside the surface:

- **Tags are permanent and append-only.** A renumbered tag does not fail to render; it
  renders the *wrong widget* on a client one version behind.
- **A parameter this client cannot read is not always harmless.** One marked
  **affordance** governs what the user is allowed to do — `enabled`, `checked`,
  `readOnly` — so a client meeting an unreadable one **withholds the whole widget**
  rather than drawing a control that lies about what it will do. Every other kind of
  skew degrades cosmetically. The components that own one are marked below.

**Reserved local tags: 10, 11.** Those
belong to hand-written bindings in this same segment, not to the generator. They
appear in no table below and are not free.

## Components

| Component | Local tag | Widget tag | Owns an affordance |
| --- | ---: | ---: | :---: |
| [`PrimaryButton`](#primarybutton) | 1 | 16777217 | yes |
| [`AsyncImage`](#asyncimage) | 2 | 16777218 |  |
| [`Card`](#card) | 3 | 16777219 |  |
| [`Badge`](#badge) | 4 | 16777220 |  |
| [`Divider`](#divider) | 5 | 16777221 |  |
| [`Chip`](#chip) | 6 | 16777222 | yes |
| [`Price`](#price) | 7 | 16777223 |  |
| [`StarRating`](#starrating) | 8 | 16777224 |  |
| [`SectionHeader`](#sectionheader) | 9 | 16777225 |  |
| [`Icon`](#icon) | 12 | 16777228 |  |
| [`TextInput`](#textinput) | 13 | 16777229 | yes |
| [`Presence`](#presence) | 14 | 16777230 |  |
| [`ScrollArea`](#scrollarea) | 15 | 16777231 |  |
| [`SnackbarArea`](#snackbararea) | 16 | 16777232 |  |
| [`Dialog`](#dialog) | 17 | 16777233 |  |
| [`SheetArea`](#sheetarea) | 18 | 16777234 |  |

---

### PrimaryButton

Local tag 1, widget tag `16777217`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `label` | `TextValue` | 1 | required | resolved by the host at draw time |
| `enabled` | `Boolean` | 2 | `true` | **affordance** |

**Events**

| Name | Signature | Tag | Notes |
| --- | --- | ---: | --- |
| `onClick` | `() -> Unit` | 1 |  |

Takes a modifier chain.

**Owns an affordance** (`enabled`), so a client that meets a property it cannot read on this component withholds it entirely.

---

### AsyncImage

Local tag 2, widget tag `16777218`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `url` | `String` | 1 | required |  |
| `contentDescription` | `String?` | 2 | `null` |  |
| `cornerRadiusDp` | `Int` | 3 | `8` | range 0..1.7976931348623157E308, clamped |

Takes a modifier chain.

---

### Card

Local tag 3, widget tag `16777219`.

**Content slots**

| Name | Tag |
| --- | ---: |
| `content` | 1 |

Takes a modifier chain.

---

### Badge

Local tag 4, widget tag `16777220`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `text` | `TextValue` | 1 | required | resolved by the host at draw time |
| `selected` | `Boolean` | 2 | `false` |  |

Takes a modifier chain.

---

### Divider

Local tag 5, widget tag `16777221`.

Takes a modifier chain.

---

### Chip

Local tag 6, widget tag `16777222`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `text` | `TextValue` | 1 | required | resolved by the host at draw time |
| `selected` | `Boolean` | 2 | `false` | **affordance** |

**Events**

| Name | Signature | Tag | Notes |
| --- | --- | ---: | --- |
| `onSelectedChange` | `(Boolean) -> Unit` | 1 |  |

Takes a modifier chain.

**Owns an affordance** (`selected`), so a client that meets a property it cannot read on this component withholds it entirely.

---

### Price

Local tag 7, widget tag `16777223`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `price` | `TextValue` | 1 | required | resolved by the host at draw time |
| `leadingText` | `TextValue?` | 2 | `null` | resolved by the host at draw time |
| `previousPrice` | `TextValue?` | 3 | `null` | resolved by the host at draw time |
| `trailingText` | `TextValue?` | 4 | `null` | resolved by the host at draw time |

Takes a modifier chain.

---

### StarRating

Local tag 8, widget tag `16777224`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `rating` | `Float` | 1 | required | range 0..5, clamped |
| `label` | `TextValue?` | 2 | `null` | resolved by the host at draw time |

Takes a modifier chain.

---

### SectionHeader

Local tag 9, widget tag `16777225`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `title` | `TextValue` | 1 | required | resolved by the host at draw time |
| `description` | `TextValue?` | 2 | `null` | resolved by the host at draw time |

Takes a modifier chain.

---

### Icon

Local tag 12, widget tag `16777228`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `name` | `String` | 1 | required |  |
| `contentDescription` | `String?` | 2 | `null` |  |
| `sizeDp` | `Int` | 3 | `24` | range 1..1.7976931348623157E308, clamped |
| `tint` | `Color?` | 4 | `null` | resolved by the host at draw time |

Takes a modifier chain.

---

### TextInput

Local tag 13, widget tag `16777229`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `text` | `String` | 1 | required |  |
| `version` | `Int` | 2 | `0` |  |
| `label` | `TextValue?` | 3 | `null` | resolved by the host at draw time |
| `placeholder` | `TextValue?` | 4 | `null` | resolved by the host at draw time |
| `enabled` | `Boolean` | 5 | `true` | **affordance** |
| `singleLine` | `Boolean` | 6 | `true` |  |
| `maxLength` | `Int` | 7 | `-1` |  |
| `mask` | `String?` | 8 | `null` |  |
| `keyboard` | `String?` | 9 | `null` |  |
| `showCounter` | `Boolean` | 10 | `false` |  |
| `focusRequested` | `Boolean` | 11 | `false` when absent | from the `focus` holder (`requested`) |
| `focusSequence` | `Int` | 12 | `0` when absent | from the `focus` holder (`sequence`) |

**Events**

| Name | Signature | Tag | Notes |
| --- | --- | ---: | --- |
| `onValueChange` | `(String, Int) -> Unit` | 1 |  |

Takes a modifier chain.

**Owns an affordance** (`enabled`), so a client that meets a property it cannot read on this component withholds it entirely.

---

### Presence

Local tag 14, widget tag `16777230`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `visible` | `Boolean` | 1 | required |  |
| `enter` | `String?` | 2 | `null` |  |
| `exit` | `String?` | 3 | `null` |  |

**Content slots**

| Name | Tag |
| --- | ---: |
| `content` | 1 |

**Events**

| Name | Signature | Tag | Notes |
| --- | --- | ---: | --- |
| `onExited` | `(() -> Unit)?` | 1 |  |

Takes a modifier chain.

---

### ScrollArea

Local tag 15, widget tag `16777231`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `horizontal` | `Boolean` | 1 | `false` |  |
| `scrollTargetDp` | `Int` | 2 | `0` when absent | from the `scroll` holder (`targetDp`) |
| `scrollSequence` | `Int` | 3 | `0` when absent | from the `scroll` holder (`targetSequence`) |
| `scrollAnimated` | `Boolean` | 4 | `false` when absent | from the `scroll` holder (`targetAnimated`) |
| `scrollWatching` | `Boolean` | 5 | `false` when absent | from the `scroll` holder (`watching`) |
| `scrollQuantumDp` | `Int` | 6 | `48` when absent | from the `scroll` holder (`reportEveryDp`) |

**Content slots**

| Name | Tag |
| --- | ---: |
| `content` | 1 |

**Events**

| Name | Signature | Tag | Notes |
| --- | --- | ---: | --- |
| `scrollReport` | `(Int, Int, Boolean) -> Unit` | 1 | the host's report back into the `scroll` holder |

Takes a modifier chain.

---

### SnackbarArea

Local tag 16, widget tag `16777232`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `snackbarsMessage` | `String` | 1 | `""` when absent | from the `snackbars` holder (`message`) |
| `snackbarsActionLabel` | `String` | 2 | `""` when absent | from the `snackbars` holder (`actionLabel`) |
| `snackbarsSequence` | `Int` | 3 | `0` when absent | from the `snackbars` holder (`sequence`) |
| `snackbarsWatching` | `Boolean` | 4 | `false` when absent | from the `snackbars` holder (`watching`) |

**Content slots**

| Name | Tag |
| --- | ---: |
| `content` | 1 |

**Events**

| Name | Signature | Tag | Notes |
| --- | --- | ---: | --- |
| `snackbarsReport` | `(Int, Boolean) -> Unit` | 1 | the host's report back into the `snackbars` holder |

Takes a modifier chain.

---

### Dialog

Local tag 17, widget tag `16777233`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `visible` | `Boolean` | 1 | required |  |
| `dismissOnBackPress` | `Boolean` | 2 | `true` |  |
| `dismissOnClickOutside` | `Boolean` | 3 | `true` |  |

**Content slots**

| Name | Tag |
| --- | ---: |
| `content` | 1 |

**Events**

| Name | Signature | Tag | Notes |
| --- | --- | ---: | --- |
| `onDismissRequest` | `() -> Unit` | 1 |  |

Takes a modifier chain.

---

### SheetArea

Local tag 18, widget tag `16777234`.

**Properties**

| Name | Type | Tag | Default | Notes |
| --- | --- | ---: | --- | --- |
| `sheetTarget` | `String` | 1 | `"hidden"` when absent | from the `sheet` holder (`targetState`) |
| `sheetSequence` | `Int` | 2 | `0` when absent | from the `sheet` holder (`targetSequence`) |
| `sheetWatching` | `Boolean` | 3 | `false` when absent | from the `sheet` holder (`watching`) |
| `sheetSkipPartial` | `Boolean` | 4 | `false` when absent | from the `sheet` holder (`skipPartiallyExpanded`) |

**Content slots**

| Name | Tag |
| --- | ---: |
| `content` | 1 |

**Events**

| Name | Signature | Tag | Notes |
| --- | --- | ---: | --- |
| `sheetReport` | `(String, Boolean) -> Unit` | 1 | the host's report back into the `sheet` holder |

Takes a modifier chain.

