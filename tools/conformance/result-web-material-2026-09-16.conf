CONF B6 PASS -- workerCreated=False, refused=DictionarySkew, said='delivery refused: the payload was built against a dictionary this client does not implement; unknown segments: androidx.material3 no Worker was created'
CONF B6-control PASS -- the same manifest renders on a client that has the tier: m3.section=buttons
CONF M1 PASS -- 10/10 sections; missing []
CONF M2 PASS -- m3.buttons=0/0/0/0/0 -> m3.buttons=1/0/0/0/0
CONF M6 PASS -- 6 icon-only controls announce a description: ['Save this', 'Bookmark this', 'Search', 'Keep']
CONF M3 PASS -- 5 unnamed controls on the section; [('m3.checkbox=', 'm3.checkbox=off', 'm3.checkbox=on'), ('m3.switch=', 'm3.switch=on', 'm3.switch=off')]
CONF M3-announced SKIP -- this client publishes Material 3's selection controls as unnamed `button` nodes with no state: a screen reader is told neither what the control is nor whether it is on. Compose's web accessibility bridge, not the generated binding; see tools/upstream-reports/README.md
CONF M7 SKIP -- this client publishes no accessibility node at all for a Material 3 Slider, so there is nothing for an assistive technology to find or move. Compose's web accessibility bridge; see tools/upstream-reports/README.md
CONF M5 PASS -- sheet=Fare conditions, menu=m3.menu=Business
CONF M4 PASS -- the dialog published ['Cancel booking', 'Cancel this booking?', 'Dogwood web slice', 'Keep it', 'The fare is refundable until midnight.']
CONF M4-operable SKIP -- with a Compose dialog open this client answers no input from outside the process: not a click on the accessibility node, not a mouse event at its box, not Escape. The binding is covered by the tier's render tests on three targets; the platform behaviour is recorded in tools/upstream-reports/README.md
CONF RESULT client=web passed=8 failed=0 skipped=3
